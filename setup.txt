1) Create three files client, backend, and blockchain
2) Move to the blockchain directory and install truffle "npm install -g truffle"
   Truffle is a development environment for Ethereum smart contracts